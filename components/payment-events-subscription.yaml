apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: payment-events-subscription
spec:
  topic: payment-events
  routes:
    default: /api/v1/dapr/payment-events
  pubsubname: kafka-pubsub
  metadata:
    isRawPayload: "true"
scopes:
  - payment-kafka-consumer-service